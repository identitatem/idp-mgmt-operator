apiVersion: apps/v1
kind: Deployment
metadata:
  name: webhook-service
  labels:
    control-plane: webhook-service
spec:
  selector:
    matchLabels:
      control-plane: webhook-service
  replicas: 1
  template:
    metadata:
      labels:
        control-plane: webhook-service
    spec:
      containers:
      - args:
        - webhook
        - "--secure-port=6443"
        - "--tls-cert-file=/serving-cert/tls.crt"
        - "--tls-private-key-file=/serving-cert/tls.key"
        - -v 5
        image: quay.io/identitatem/idp-mgmt-operator@sha256:9907ca2c5d3854fd13696c379b2338926131c46b89ef40bc80fa0e3fef4b5faf
        name: webhook
        imagePullPolicy: Always
        volumeMounts:
          - name: webhook-secret
            mountPath: "/serving-cert"
            readOnly: true
      volumes:
        - name: webhook-secret
          secret:
            secretName: idp-mgmt-webhook
      serviceAccountName: idp-mgmt-operator-webhook-service
      terminationGracePeriodSeconds: 10
